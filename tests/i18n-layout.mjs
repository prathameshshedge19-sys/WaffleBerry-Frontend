import {readFile} from 'node:fs/promises';import{resolve,extname}from'node:path';import{fileURLToPath,pathToFileURL}from'node:url';
const{chromium}=await import(pathToFileURL(process.env.L19_PLAYWRIGHT_MODULE));const root=fileURLToPath(new URL('../',import.meta.url));const browser=await chromium.launch({headless:true,channel:process.env.GUIDE_BROWSER_CHANNEL||undefined});
try{const page=await browser.newPage({viewport:{width:320,height:900}});await page.route('**/*',async route=>{const u=new URL(route.request().url());if(u.hostname!=='layout.test')return route.abort();if(/\.m?js$/.test(u.pathname)&&!/\/(i18n|product-guide)\.js$/.test(u.pathname))return route.fulfill({body:'',contentType:'text/javascript'});try{return route.fulfill({body:await readFile(resolve(root,'.'+u.pathname)),contentType:({'.html':'text/html','.css':'text/css','.js':'text/javascript','.json':'application/json','.svg':'image/svg+xml'})[extname(u.pathname)]||'application/octet-stream'});}catch{return route.fulfill({status:404,body:''});}});
for(const locale of ['de','fr','hi','mr']){await page.goto('https://layout.test/index.html');await page.evaluate(locale=>window.LegaryaI18n.setLanguage(locale),locale);console.log(locale,await page.evaluate(()=>[...document.querySelectorAll('body *')].filter(e=>{const r=e.getBoundingClientRect();return r.width&&r.right>innerWidth+1&&getComputedStyle(e).visibility!=='hidden'&&!e.closest('svg');}).map(e=>({tag:e.tagName,cls:e.className,text:e.textContent.slice(0,70),right:e.getBoundingClientRect().right})).slice(0,18)));if(process.env.I18N_SCREENSHOTS)await page.screenshot({path:resolve(process.env.I18N_SCREENSHOTS,`layout-${locale}-320.png`)});}}
finally{await browser.close();}
